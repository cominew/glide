# Task Priority Rules

- User explicit instruction > Scheduled task > Background reflection
- Customer query priority > internal report generation
- When multiple pending goals exist, execute in descending order of `priority` field (managed by GoalEngine)