// kernel/kernel.ts
// ─────────────────────────────────────────────
// Glide Event Kernel Definition
// Runtime-free. Instance-free. Center-free.
// ─────────────────────────────────────────────

/**
 * Glide does not start.
 * Glide does not run.
 * Glide is the condition under which events appear.
 */

export type GlideKernel = {

  /** Reality exists as event trace */
  readonly events: true

  /** Phenomena arise from observation */
  readonly arising: true

  /** Boundaries translate events */
  readonly gateways: true

  /** Observation collapses field */
  readonly ritual: true

  /** Field manifestation */
  readonly field: true
}

/**
 * Kernel is declaration, not construction.
 */
export const Kernel: GlideKernel = {
  events: true,
  arising: true,
  gateways: true,
  ritual: true,
  field: true
}